
* Kubernetes doesn't manage user accounts natively but rather it relies on external (ex: *certificates*, *files with user details*)
* Kubernetes can create and manage service accounts

## User Authentication

* All user access is managed by the kube-apiserver