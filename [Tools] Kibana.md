# Kubernetes: Kibana

Kibana is a web-based UI that allows users to visualize logs and other data stored in Elasticsearch

* Part of the EFK stack (*Elasticsearch*, *Fluentd*, *Kibana*)
* Allows users to explore logs indexed in Elasticsearch