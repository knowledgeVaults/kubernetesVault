# Kubernetes: Istio Envoy Proxies

Envoy Proxy is a code data plane component in Istio service meshes that acts as a high-performant sidecar proxy 

* Injected alongside the main application pod to handle all inbound and outbount traffic transparently