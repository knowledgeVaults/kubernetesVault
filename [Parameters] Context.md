# Kubernetes: Context

Context is a set of access parameters that tells `kubectl` which cluster to talk to, as which user, and in which namespace by default

* Stored in the kubeconfig file (`~/.kube/config`)
* Allows users to quickly switch between environments without re-specifying flags everytime