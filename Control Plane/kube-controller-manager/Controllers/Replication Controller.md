
A `ReplicationController` ensures that a specific number of identical Pods are running at all times by continuously checking the cluster's current state 

* Creates or deletes Pods to help maintain the desired replica count