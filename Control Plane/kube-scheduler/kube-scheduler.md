
The `kube-scheduler` is the default scheduler that assigns unscheduled Pods to suitable nodes

* Watches the API server for new Pods that aren't assigned to any node
* Decides which node to place the Pod in based on resource needs and policies

