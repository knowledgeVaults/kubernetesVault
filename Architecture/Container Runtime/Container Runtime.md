
A container runtime is the software that actually runs containers on a machine (*Pull container images*, *Create isolated container processes*, *Manage container lifecycles*, *etc.*)

* Must be installed on every master and worker nodes in a Kubernetes cluster