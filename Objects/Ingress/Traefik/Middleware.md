
Traefik uses HTTP middlewares for authentication configured via CRDs like Middleware and applied to IngressRoute or Ingress resources

* They secure routes by challenging clients for credentials or delegating to external services
